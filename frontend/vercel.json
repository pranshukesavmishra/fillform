{
  "buildCommand": "echo 'pre-built locally, nothing to build'",
  "installCommand": "echo 'no install needed for a static export'",
  "outputDirectory": "build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
